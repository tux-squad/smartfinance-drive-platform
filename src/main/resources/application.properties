spring.application.name=smartfinance-drive-platform

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:jzoqodzv}
cloudinary.api-key=${CLOUDINARY_API_KEY:767526792535413}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:6BREPsPImIm4f3tRPdEMEC0SH1I}

# Multipart File Upload Limits (Max 10MB)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:4200,http://localhost:5173,http://localhost:8080}

# Security / JWT Authorization Configuration
authorization.jwt.secret=${JWT_SECRET:SmartFinanceDrivePlatformSecretKeyForJwtTokenGenerationAndValidation2026!}
authorization.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
authorization.jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:604800000}

# Google OAuth2 Credentials
google.client-id=${GOOGLE_CLIENT_ID:}
google.client-secret=${GOOGLE_CLIENT_SECRET:}

# Database Configuration (PostgreSQL - Aiven Cloud)
spring.datasource.url=${DB_URL:jdbc:postgresql://smartfinance-mark.e.aivencloud.com:11878/defaultdb?sslmode=require}
spring.datasource.username=${DB_USERNAME:avnadmin}
spring.datasource.password=${DB_PASSWORD:AVNS_HWl9hTctofPAOU_Ai-}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
